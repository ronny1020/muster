## What this changes

<!-- The behaviour, not the diff. One or two sentences. -->

## Checks

<!-- CONTRIBUTING.md lists all six; the pre-commit hook runs most of them. -->

- [ ] `bun run check` and `bun test`
- [ ] `bun run format:check`
- [ ] `cargo fmt --check`, `cargo test --lib` and `cargo clippy --all-targets`

## Verified by hand

<!-- Required for anything touching a PTY, process spawning, the window
     chrome, or a platform CI cannot test. Say what you ran and what you saw —
     or say plainly that you did not run it. A green suite does not exercise a
     terminal. -->

## Anything a reviewer should know

<!-- An invariant you had to work around, a decision with two defensible
     answers, a platform you could not test. -->
